{
  "name": "toy-react",
  "version": "0.0.1",
  "description": "toy react 开发",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node build.ts"
  },
  "author": {
    "name": "jump__jump"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.8.2",
    "@types/node": "^14.14.6"
  }
}
